#include <iostream>
#include <math.h>
#include <vector>

#define rp(i, n) for(ll i=0; i < n; i++)

using namespace std;

typedef long long ll;

int main() {
    rp(i, 30) {
        cout << i << endl;
    }
    return 0;
}